# Sample environment variables for LokanOS development.
# Replace these placeholder values with safe local development credentials.
LOKAN_SECRETS_BACKEND=env
SERVICE_API_TOKEN=replace-with-dev-token
SERVICE_URL=https://localhost:8443
