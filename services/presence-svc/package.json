{
  "name": "@lokanos/presence-svc",
  "version": "0.1.0",
  "description": "In-memory presence tracking service for LokanOS.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "build": "node -e \"console.log('build step not required for presence service')\"",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "jest": "^30.1.3",
    "supertest": "^7.1.4"
  },
  "jest": {
    "testEnvironment": "node",
    "collectCoverage": true,
    "coverageProvider": "v8"
  }
}
