{
  "name": "lokanos-api",
  "version": "0.1.0",
  "private": true,
  "description": "API definitions and tooling for LokanOS",
  "scripts": {
    "generate": "echo \"OpenAPI generation placeholder\"",
    "lint": "redocly lint openapi.yaml",
    "validate": "npm run lint"
  },
  "devDependencies": {
    "@redocly/cli": "^1.16.0"
  }
}
